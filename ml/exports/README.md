# Export Workspace

Future exported model artifacts can live here, such as:

- `.tflite` files
- label maps
- evaluation summaries
- quantized export notes

Do not commit large model binaries unless the project intentionally decides to version them.
